---

# controller_credentials:

# - name: OPA Server
#   organization: Default
#   credential_type: Open Policy Agent Server
#   inputs:
#     opa_server: "{{ controller_credential_opa_server }}"
#     opa_token: "{{ controller_credential_opa_token }}"
#     opa_validate_certs: false